---
props:
  projects: []

---

import { siteConfig } from "../config/site";

# Projects

See more projects on my [GitHub](https://github.com/yourusername).

---

Here are some of my favorite projects:

<ul>
  {props.projects.map((project) => (
    <li>
      <strong><a href={`${siteConfig.links.github}/${project.slug}`}>{project.name}</a></strong>: {project.description}
    </li>
  ))}
</ul>

