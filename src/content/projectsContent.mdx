import { siteConfig } from "../config/site";
import ProjectLink from "@/components/ui/ProjectLink.astro";

# Projects

Check out my [GitHub](${siteConfig.links.github}) for more projects and contributions.

---

Here are some of my favorite projects:

<ul>
  {props.projects.map((project) => (
    <ProjectLink
      slug={project.slug}
      name={project.name}
      description={project.description}
      link={project.link}
      tech={project.tech}
      type={project.type}
    />
  ))}
</ul>
