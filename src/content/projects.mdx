---
props:
  projects: []

---

import { siteConfig } from "../config/site";

# Projects

See more projects on my [GitHub](https://github.com/yourusername).

---

Here are some of my favorite projects btw:

<ul>
  {props.projects.map((project) => (
    <li>
      <a href={`${siteConfig.links.github}/${project.slug}`}>{project.name}</a>: {project.description}
    </li>
  ))}
</ul>

