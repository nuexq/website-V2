#  {props.title}

<p class="text-sm !mt-1">
  <time dateTime={props.date.isoStringDate}>{props.date.formattedDate}</time>
  {props.edited && (
    <time dateTime={props.edited.isoStringEdited} class="!text-muted-foreground">
      {" "}(last edited {props.edited.formattedEdited})
    </time>
  )}
</p>
---
