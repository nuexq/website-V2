import { siteConfig } from "../config/site";
import ProjectLink from "@/components/ui/ProjectLink.astro";

# Projects

Check out my [GitHub](${siteConfig.links.github}) for more projects and contributions.

---

<ul class="grid grid-cols-1 md:grid-cols-2 gap-[2em_1.5rem] !mt-5 !pl-0 !pt-0 prose-li:p-0">
  {props.projects.map((project) => (
    <ProjectLink
      slug={project.slug}
      name={project.name}
      description={project.description}
      link={project.link}
      tech={project.tech}
      type={project.type}
    />
  ))}
</ul>
